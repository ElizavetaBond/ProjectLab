
@{
    ViewData["Title"] = "Fill";
    Layout = "~/Views/Shared/_Layout.cshtml";
    @model List<ComponentBrowseProjectViewModel>
}

<div class="background">
    <form asp-action="Fill" asp-controller="Project" method="post">
        <div class="container">
            @for (var i = 0; i < Model.Count; i++)
            {
                <div class="row">
                    <div class="col-3">
                        <label>@Model[i].Name</label>
                    </div>
                    <div class="col-8">
                        @if (Model[i].ComponentType == "Текст" || Model[i].ComponentType == "Сообщение")
                        {
                            <input type="text" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Дата")
                        {
                            <input type="date" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Время")
                        {
                            <input type="time" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Число")
                        {
                            <input type="number" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Флаг")
                        {
                            <input type="checkbox" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Файл")
                        {
                            <input type="file" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Фото")
                        {
                            <input type="file" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Место")
                        {
                            <input type="text" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Выбор")
                        {
                            <input type="text" name="vm[@i].Value" class="form-control" />
                        }
                        else if (Model[i].ComponentType == "Гиперссылка")
                        {
                            <input type="url" name="vm[@i].Value" class="form-control" />
                        }
                    </div>
                </div>
            }
            <button type="submit" class="btn btn-primary">Загрузить</button>
        </div>
    </form>
</div>

